# Dev / test-only dependencies. Install with:
#   pip install -r requirements-dev.txt
#
# Kept separate from app/requirements.txt so production deploys don't pull
# pytest, mocks, etc.
pytest>=8.0
pytest-flask>=1.3

# Cypher-runner MCP server (app/database/cypher_runner/mcp) — local dev tool that
# exposes the curated query registry to MCP clients. Not needed in production.
# neo4j + pyyaml are already runtime deps in app/requirements.txt.
mcp[cli]>=1.28
